<nav class="navbar sticky-top navbar-expand-lg bg-body-tertiary bg-dark" data-bs-theme="dark">
    <div class="container-fluid">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarTogglerDemo01"
            aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
            <a class="navbar-brand" href="{{ url_for('main.home') }}">AnimalWars<em>
                    <font color="red">2086</font>
                </em></a>
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" href="{{ url_for('main.home') }}"> Home </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ url_for('main.about') }}"> About </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ url_for('auth.purchase') }}"> Purchase </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ url_for('auth.commission') }}"> Commission Work </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ url_for('auth.contact') }}"> Contact </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ url_for('auth.signin') }}"> Sign-In </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ url_for('auth.register') }}"> Register </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ url_for('social.post') }}"> Posts </a>
                </li>
            </ul>
            <ul>
                <li class="nav-item" style="list-style-type:none;">
                    <a class="nav-link" href="{{ url_for('social.user_page', username=current_user.username) }}">My
                        Page</a>
                </li>
                <li class="nav-item" style="list-style-type:none;">
                    <a class="nav-link" href="{{ url_for('auth.logout') }}">Logout</a>
                </li>
            </ul>
            <ul style="list-style-type:none;">
                <li>
                </li>
            </ul>
            <form class="d-flex" role="search">

                <input class="form-control me-2" type="search" placeholder="Search Animal Wars" aria-label="Search">
                <button class="btn btn-outline-success" type="submit">Search</button>
            </form>
        </div>
    </div>
</nav>